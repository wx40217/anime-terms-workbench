# 分阶段验证并限制第一版生效范围

术语库使用标识 `anime`，首版只发布 `zh-CN` 且不使用 `auto`；通过 `matches` 限制在经真实页面审查的约 5 至 10 个稳定动漫资讯、行业媒体或资料站规则上生效，证据来源与站点白名单独立评估。首次实现先用覆盖七类的 15 至 25 条术语和 2 至 3 个候选站点验证元数据、旁证结构、自动检查及真实页面效果，不加入交付仓库的公共索引；扩充至 80 至 120 条后，必须通过结构与证据自动检查、每类至少两个真实页面的人工抽查以及维护者审核才能发布，后续术语或站点变更也必须同步更新旁证和测试依据。
